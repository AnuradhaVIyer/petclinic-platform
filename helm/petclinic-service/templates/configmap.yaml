apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "petclinic-service.name" . }}
  labels:
{{- include "petclinic-service.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ default "" $value | toString | quote }}
{{- end }}
